"use strict"; // treat all JS code as newer version of JS and now you can't switch to older version

let name = "Vijay"
let age = "18"
let isLoggedIn = false

/*
DataTypes ->

    Primitive DataTypes ->

        number => (range -> aproximate 2 power 53)
        bigint => (for very big numbers)
        boolean =>  true / false
        string => ""

        null => (standalone value / represent of empty value)
        undefined => itself a datatype (value not initilized)
        symbol => (for specify uniqueness)

    Non Primitive DataTypes -> 

        object

*/

console.log(typeof undefined);